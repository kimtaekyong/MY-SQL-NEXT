// 서버 컴포넌트에서 데이터 가져오기
export default async function Dashboard() {
  // 데이터 렌더링
  return (
    <div className="h-[100vh] flex justify-center items-center flex-col">
      <h2 className="text-2xl">로그인 완료되셨습니다.</h2>
    </div>
  );
}
